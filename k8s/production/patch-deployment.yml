apiVersion: apps/v1
kind: Deployment
metadata:
  name: course-reservations
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: app
          image: ghcr.io/podzimekmartin/coursereservations:1.0.0
